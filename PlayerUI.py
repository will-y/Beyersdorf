import math
import pygame as pg

class PlayerUI():
    """his class is the class that constructs and makes all the things that the 
    user sees and interacts with such as the resources bar on the right side of 
    the screen and other various things that are yet to be added"""
    
    def __init__(self,screen):
        self.screen = screen
    
    def drawRightUI(self):
        """Draws the player UI on the right side of the screen that shows the 
        character their current resources"""
        p = "DFSfsdFDS"
        p = p.
        pass

